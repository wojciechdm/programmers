import { EmployeeRoleLevelPipe } from './employee-role-level.pipe';

describe('EmployeeRoleLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeRoleLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
