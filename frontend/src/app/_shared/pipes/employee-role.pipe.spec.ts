import { EmployeeRolePipe } from './employee-role.pipe';

describe('EmployeeRolePipe', () => {
  it('create an instance', () => {
    const pipe = new EmployeeRolePipe();
    expect(pipe).toBeTruthy();
  });
});
